# Decisions

| ID | Date | Decision | Why it is locked now |
| --- | --- | --- | --- |
| D-001 | 2026-03-12 | AGIF v1 lives in this standalone workspace root. | Keeps this initiative separate from the old repo and avoids mixed ownership. |
| D-002 | 2026-03-12 | The old repo `/Users/ahsadin/Documents/Projects/ENF/AGIF/agif-tasklet-cell` is reference-only. | AGIF v1 must not depend on the old repo at runtime. |
| D-003 | 2026-03-12 | Phase 0 is limited to bootstrap structure and documentation. | Prevents accidental runtime implementation before requirements and design gates. |
| D-004 | 2026-03-12 | The proof domain is document/workflow intelligence, with finance documents as the first real tissue system. | Keeps the first proof domain focused while preserving the broader AGIF vision. |
| D-005 | 2026-03-12 | Progress uses a fixed denominator of `600` units with the locked phase weights recorded in planning docs. | Keeps status reporting stable across all future threads. |
| D-006 | 2026-03-12 | AGIF v1 preserves the full AGIF architecture conceptually, but does not claim to prove long-horizon AGI-like properties in v1. | Protects the broad vision without overstating what v1 demonstrates. |
| D-007 | 2026-03-12 | Resource targets are capped to the Apple M4 MacBook Air profile: `<= 12 GB` runtime working set and `<= 35 GB` total project/evidence footprint. | The system must be architecturally complete and still fit the primary local machine target. |
| D-008 | 2026-03-12 | Phase 1 freezes the proof domain to document/workflow intelligence with the finance document workflow as the first real tissue system. | Keeps the first proof bounded and measurable while preserving the wider AGIF path. |
| D-009 | 2026-03-12 | AGIF v1 success requires a runnable local prototype, research paper, benchmark evidence, and reproducibility package together. | Prevents partial delivery from being mislabeled as a finished v1 proof. |
| D-010 | 2026-03-12 | AGIF v1 must be judged against explicit falsification thresholds, including no old-repo runtime dependency, real multi-cell separation, bounded adaptation, governance recovery, and resource limits. | Makes future success or failure testable instead of narrative. |
| D-011 | 2026-03-12 | Phase 1 numeric thresholds are frozen as follows: catastrophic forgetting `<= 10%`, unsafe or misaligned action rate `<= 0.1%`, resource depletion/overload/repeated rollback `<= 5%` operational time, sample or compute cost to bounded target performance `<= 2x` flat baseline, runtime working set `<= 12 GB`, and total project/evidence footprint `<= 35 GB`. | Keeps the requirements aligned with the full project plan and gives later benchmark work exact pass/fail limits. |
| D-012 | 2026-03-12 | `/Users/ahsadin/Downloads/PLAN.md` is the authoritative execution plan for this repo and future work must reconcile to it before implementation starts. | Prevents drift between chat decisions and the locked project plan. |
| D-013 | 2026-03-12 | Phase 2 freezes the six design documents in `03_design/` as the gate for all later implementation work. | Prevents Phase 3 from starting while architecture, interfaces, lifecycle, authority, memory, and benchmark rules are still ambiguous. |
| D-014 | 2026-03-12 | The Phase 2 interface set is frozen exactly as written for `FabricConfig`, `CellBlueprint`, `CellRuntimeState`, `DescriptorRecord`, `NeedSignal`, `LifecycleEvent`, `MemoryPromotionDecision`, and `CellUtilityProfile`, along with the `runner/cell fabric` CLI contract. | Later threads need a stable target for implementation, tests, and evidence capture. |
| D-015 | 2026-03-12 | The only allowed v1 need-signal kinds are `overload`, `uncertainty`, `novelty`, `redundancy`, `memory_pressure`, `trust_risk`, and `coordination_gap`, and the only frozen lifecycle states are `seed`, `dormant`, `active`, `split_pending`, `consolidating`, `quarantined`, and `retired`. | Stops later threads from silently widening the adaptation or lifecycle model. |
| D-016 | 2026-03-12 | Decision authority is frozen as follows: cells propose local action, tissues evaluate local structural need, governance approves or rejects higher-risk transitions, and human policy defines the outer safety boundary. | Keeps need pressure, permission to act, and human safety limits clearly separated. |
| D-017 | 2026-03-12 | Split and merge rules are frozen as follows: split requires governance approval, merge requires tissue coordinator plus governance co-approval, children inherit lineage, role family, trust ancestry, descriptor eligibility, and policy envelope, children may refine only inside role family, no merge may complete without conflict-aware consolidation, and no lineage rewrite is allowed without a ledger record. | Protects lineage integrity and prevents structural change from becoming ad hoc during implementation. |
| D-018 | 2026-03-12 | Phase 2 freezes AGIF v1 memory to hot, warm, and cold retained tiers plus a separate evidence log store, with reviewed promotion only and raw logs excluded from long-term memory by default. | Keeps memory bounded, auditable, and compatible with replay, rollback, and the anti-bloat design goal. |
| D-019 | 2026-03-12 | Phase 2 freezes the benchmark classes to flat baseline, multi-cell fabric without bounded adaptation, and multi-cell fabric with bounded adaptation and descriptor sharing, and freezes the required benchmark metrics before code starts. | Prevents later work from redefining comparison points after seeing results. |
| D-020 | 2026-03-12 | Routine hibernation remains a first-class lifecycle concept in v1 but does not add a new `LifecycleEvent.transition` enum beyond the frozen list; it is recorded through dormant-state lifecycle records and reasons. | Reconciles the preserved AGIF lifecycle concept with the transition enum frozen by the authoritative plan. |
| D-021 | 2026-03-13 | Phase 4 stores logical population, runtime state, lifecycle history, lineage ledger, need signals, veto records, and rollback snapshots as separate bounded local JSON stores under the fabric state root. | Keeps lifecycle replayable and rollback-safe without changing the frozen Phase 2 interface field names or CLI surface. |
| D-022 | 2026-03-13 | Phase 4 treats `active_population_cap` as the steady active target and uses a governed burst cap of `48`, then automatically returns to steady `24` after consolidation. | Satisfies the local proof target of logical scale with bounded active runtime size on the locked machine profile. |
| D-023 | 2026-03-13 | Phase 5 stores reviewed memory as hot references plus warm and cold retained payloads under the local fabric state root, keeps raw logs in a separate ephemeral store, and requires every retained memory change to be recorded with the frozen `MemoryPromotionDecision` shape. | Locks the Phase 5 memory path without changing the frozen Phase 2 interface field names or the runner command surface. |
| D-024 | 2026-03-13 | Phase 5 treats memory pressure as a first-class `memory_pressure` control signal that must trigger consolidation, deduplication, demotion, retirement review, and garbage collection before cap breach. | Keeps memory density growing faster than raw memory size on the locked M4 machine envelope. |
| D-025 | 2026-03-13 | The post-Phase-5 hardening pass may add internal lifecycle usefulness metrics, compact dormancy profiles, and anti-thrashing guardrails in local runtime state as long as it does not change the frozen Phase 2 enums, field names, or CLI contract. | Strengthens lifecycle quality before Phase 6 without reopening the Phase 2 interface freeze. |
| D-026 | 2026-03-13 | The post-Phase-5 hardening pass may score reviewed memory by novelty, usefulness, trust, reuse potential, compression gain, and conflict risk, and may use that score to prefer defer/reuse/supersede paths while preserving the frozen `MemoryPromotionDecision` shape. | Improves selectivity, trust-weighted retention, and bounded growth before routing and authority depth are added in Phase 6. |
| D-027 | 2026-03-13 | Phase 6 records need history, routing decisions, and authority reviews as additive local runtime stores under the fabric state root. | Gives Phase 6 traceability for need, utility, routing, and governance without changing the frozen Phase 2 field names. |
| D-028 | 2026-03-13 | Phase 6 adds routing, authority, and utility summaries only as additive data inside the existing `fabric status`, `fabric run`, and `fabric evidence` outputs while keeping the frozen command names and fail-closed JSON envelope unchanged. | Makes the new runtime behavior inspectable without reopening the frozen CLI surface. |
| D-029 | 2026-03-13 | The Phase 6.5 hardening pass may add separate internal stores for need-resolution outcomes, routing-memory outcomes, and authority review patterns as long as the frozen `NeedSignal`, `CellRuntimeState`, `LifecycleEvent`, and CLI envelopes stay unchanged. | Adds inspectable hardening depth for recurrence, routing memory, and authority memory without silently drifting the frozen Phase 2 interfaces. |
| D-030 | 2026-03-13 | The Phase 6.5 router may abstain or escalate under explicit low-confidence conditions for Phase 6 profiles, but must preserve the older bounded multi-cell selection path for pre-Phase-6 benchmark profiles. | Keeps the Phase 6 decision layer honest about weak routes while avoiding regressions in the already-closed Phase 3 to 5 proof paths. |
| D-031 | 2026-03-13 | Phase 7 uses dedicated Phase 7 finance blueprint and tissue registries while keeping the earlier Phase 3 registry and the frozen `runner/cell fabric` command names unchanged. | Adds real domain tissues and benchmark assets without breaking the already-closed Phase 3 to 6 fixtures and contracts. |
| D-032 | 2026-03-13 | Phase 7 benchmark determinism is verified by repeated fresh-state reruns of the same deterministic suite, and split/merge efficiency remains a bounded placeholder metric until later benchmark cases intentionally trigger governed split or merge events. | Keeps the Phase 7 evidence honest and phase-bounded without drifting into Phase 8 or Phase 9 claims. |
| D-033 | 2026-03-13 | The Phase 7.5 hardening pass may add more deterministic finance cases, tissue analytics, bounded resource-delta metrics, and explicit structural-pressure signals as long as the three frozen benchmark classes, the frozen `runner/cell fabric` command surface, and the recorded project progress stay unchanged at `525/600`. | Improves benchmark credibility and inspectability before Phase 8 without silently reopening the benchmark contract or inflating progress. |
| D-034 | 2026-03-13 | Phase 8 adds a resumable soak harness, dedicated Phase 8 fixtures, and new `scripts/check_phase8_soak.py` and `scripts/run_phase8_soak.py` wrappers, but it does not change the frozen `runner/cell fabric` command names or Phase 2 interface shapes. | Keeps long-run orchestration additive and phase-bounded while preserving the already-frozen runner and runtime contracts. |
| D-035 | 2026-03-13 | Phase 8 may report `AGIF_FABRIC_P8_HARNESS_READY` for bounded local harness validation, but it may not claim `AGIF_FABRIC_P8_PASS` or raise project progress above `525/600` until real 24h and 72h local soak evidence is complete. | Prevents the new harness from being mistaken for a closed long-run proof and keeps progress reporting honest. |
| D-036 | 2026-03-13 | The Phase 7.6 hardening pass may normalize stored correction-memory vendor signatures with the same bounded OCR alias cleanup used for live finance inputs, and may add route-of-custody, descriptor-memory, confidence, and tradeoff reporting to Phase 7 benchmark artifacts as additive evidence only. | Fixes a real descriptor-matching weakness introduced by quantized memory supersession and improves benchmark explanation depth without changing the frozen `runner/cell fabric` command surface or the Phase 2 interface shapes. |
| D-037 | 2026-03-13 | The Phase 8.5 hardening pass may add additive cycle-health metrics, drift analysis, checkpoint-boundary resume diagnostics, failure taxonomy reporting, and Phase 7.6-aligned long-run fixtures as long as the frozen `runner/cell fabric` commands, Phase 2 interface shapes, project progress, and honest-open Phase 8 status all stay unchanged. | Improves the usefulness of the already-open Phase 8 evidence layer before real 24h and 72h soaks without silently claiming Phase 8 closure or widening into Phase 9. |
| D-038 | 2026-03-15 | Phase 8 machine roles must stay explicit: the MacBook Air is the main development and documentation machine plus the primary target profile, while MSI long-run soak artifacts count only as MSI endurance evidence and must not be described as MacBook Air-only proof. | Keeps long-run evidence honest while preserving the locked MacBook target and the open Phase 8 closure rules. |
| D-039 | 2026-03-18 | The completed real `24h` and real `72h` MSI soak artifacts, together with the locally passing bounded Phase 8 harness check, satisfy the recorded Phase 8 exit gate and therefore justify `AGIF_FABRIC_P8_PASS` and recorded progress `570/600`, provided the closure docs state two caveats explicitly: MSI provenance is not MacBook Air-only endurance proof, and the repeated `WinError 5` manifest-write interruption in the `72h` run left incomplete resume bookkeeping in the final manifest. | Closes Phase 8 honestly against the locked gate while preserving the machine-role distinction and documenting the one recurring artifact-quality weakness instead of hiding it. |
