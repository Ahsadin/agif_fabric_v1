# Decisions

| ID | Date | Decision | Why it is locked now |
| --- | --- | --- | --- |
| V1X-D-001 | 2026-03-18 | Track B lives under `projects/agif_v1_postclosure_extensions/` as a separate initiative inside the AGIF v1 repo. | Keeps extension work organized without reopening the closed AGIF v1 phase history. |
| V1X-D-002 | 2026-03-18 | Extension progress uses fixed denominator `130` only inside this project folder. | Prevents extension work from altering the closed AGIF v1 `600/600` record. |
| V1X-D-003 | 2026-03-18 | Extension tokens are tracked only inside this project folder and not in the root AGIF v1 pass-token file. | Keeps the closed v1 proof and the extension evidence clearly separated. |
| V1X-D-004 | 2026-03-18 | The ordered dependency chain is setup -> organic load -> skill graph -> POS domain -> bundle close. | Prevents later proof claims from getting ahead of missing prerequisites. |
| V1X-D-005 | 2026-03-18 | Root AGIF v1 remains finance-only proof for the closed v1 claim even after extension work starts. | Protects the already-closed v1 research story from scope drift. |
| V1X-D-006 | 2026-03-18 | Root `01_plan/PROGRESS_TRACKER.md` remains frozen at `600/600`, and Track B progress may be recorded only inside the Track B project folder. | Prevents extension bookkeeping from silently rewriting the closed AGIF v1 status. |
| V1X-D-007 | 2026-03-18 | Gap 1 is frozen as one deterministic `40`-case stream shared by both the elastic run and the no-split control run in the same order, with no fake stress-mode switch, governance-level split disable in the control run, and automatic gate failure if no organic split occurs inside that stream. | Preserves the already-agreed organic-load proof rules before any runtime work begins. |
| V1X-D-008 | 2026-03-18 | Gap 3 is frozen as the same `5` deterministic POS cases in the same order for both runs, with governance-level cross-domain transfer disable in the control run and explicit `transfer_approval` required for any counted cross-domain influence. | Preserves the already-agreed POS comparison rules before any domain work begins. |
| V1X-D-009 | 2026-03-18 | The final extension bundle verifier must run Gap 1, then Gap 2, then Gap 3, and must also re-check the unchanged root AGIF v1 closure state. | Freezes the proof order so later bundle closure cannot skip prerequisites or ignore the root safety check. |
| V1X-D-010 | 2026-03-18 | Gap 1 is proven only by one deterministic `40`-case finance stream with near-capacity runtime caps (`10` steady active, `12` burst), an elastic run that may use the existing lifecycle split or merge path, and a governance-disabled no-split control run using the exact same ordered cases. | Keeps the proof tied to normal workload pressure instead of a fake stress switch and preserves a fair same-stream comparison. |
| V1X-D-011 | 2026-03-18 | Gap 1 usefulness is judged by deterministic queue and end-to-end latency units plus accuracy preservation, not by split activity alone. | Prevents the extension from claiming success if a split occurs but does not improve useful workload behavior. |
